name: Work Session GitHub Preflight

on:
  workflow_dispatch:
  push:
    branches:
      - preflight/work-session-write-check
  pull_request:
    branches:
      - main

jobs:
  preflight:
    runs-on: ubuntu-24.04
    steps:
      - name: Print preflight result
        run: echo "GitHub Actions preflight passed"
